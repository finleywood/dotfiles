# Finley's dotfiles

## How to install

1. Clone repo
2. Run `chmod +x ./install.sh` and then `./install.sh`

This will symlink files from here to your home `~` dir. It will ask before any overwrites needed.
